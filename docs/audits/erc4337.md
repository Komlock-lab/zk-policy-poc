---
id: audit-03
type: audit
title: ERC-4337 Epic audit
epic: epic-03
status: passed
date: 2026-09-05
---

# ERC-4337 Epic audit

## 実行した検証

- Story PR #11・#12の統合commit `58cb635`に対して、rootが固定lockfileによる再installと全品質ゲートを再実行した。
- `pnpm test`: build/typecheck、Circuit 4件、Contract 28件（fuzz含む）、TypeScript unit 73件、E2E 18件成功。
- `pnpm benchmark:circuit`: ACIR 12、Brillig 8、Proof 7,232 bytes、生成327 ms（単発の観測値）。
- 実CLI subprocessから0.1 ETH active Policyの0.01 ETH決済が成功。実Alto safe-mode、EntryPoint v0.8、Verifier、Account、Policy API、SQLiteを使用し、receipt成功、受取人残高増加、Owner EOA nonce不変を確認した。
- 1 ETH上限超過、不正Owner署名、実送金額改ざん、Commitment改ざん、EntryPoint不一致で送金せず、EntryPoint nonceと残高が不変。非loopback API/RPC/Bundlerは接続前に拒否。
- Policy登録・更新、Token再発行、Proof freshness、Owner直接決済を含むPhase 2 E2Eの回帰なし。
- `node scripts/validate-planning.mjs`、`git diff --check`成功。Wiki変更なし。既存18ページのbroken wikilink 0件をrootが確認。
- 独立Contract/Alto監査でOwner認証、EntryPoint caller、prefund、Proof/public-input bindingを確認。署名はv0.8 raw userOpHash、nonceはkey 0、calldata/gas/chain/EntryPointへ結合する。
- Alto 0.0.21のpatchは127.0.0.1 bind、明示ローカルv0.8 address対応、tracer top-level結果の補完、同梱Pimlico simulation ABIに合わせたRETURN decodeを実施。REVERTは例外となり、欠損結果も拒否する。opcode/storage/codehash、署名、有効期限の検査は変更していない。
- TIMESTAMPを読むvalidation fixtureをsnapshot内で設定し、実Alto safe-modeが禁止opcodeとして拒否する追加E2Eが成功した。trace互換修正による安全規則の迂回がないことを実証した。
- CLIの入力失敗は非zero終了。入力Token・秘密鍵をstdout/stderrへ出力しない。Altoへ継承する環境はPATHとローカルEntryPointのみで、process出力を保存しない。

## Findings

| ID | Severity | Area | Finding | Remediation | Status |
| --- | --- | --- | --- | --- | --- |
| F-03-01 | LOW | Dependency | Alto固定版にローカル運用・simulation互換patchを保持する必要がある | versionとpatch hashをlockfile固定。上流更新時は実Alto正常・不正署名・禁止opcode E2Eを必須とする | retained |

## 再監査

- 初回Story監査: Contract/Alto独立監査とClient差分確認を実施。互換patch後に実Bundler E2E 10件、全回帰ゲートが成功。
- Client/CLI独立監査も完了し、CRITICAL/HIGH/MEDIUM/LOWの新規指摘なし。有限receipt待機、署名置換、Proof共有境界、入力検証、秘密非出力を確認した。
- 最終CLI subprocess検証も成功し、receipt pollingは60回の有限retryとした。
- Epic統合後の再検証: `pnpm install --frozen-lockfile`、`pnpm test`が成功。Circuit 4件、Contract 28件、unit 73件、E2E 18件。実CLIによる0.01 ETH決済と全異常系・禁止opcode拒否が再度成功。
- 統合後benchmark: ACIR 12、Brillig 8、Proof 7,232 bytes、生成322 ms（単発観測）。生成Verifierの差分なし。
- 独立監査3領域（Contract/ZK、Alto patch、Client/CLI）を完了。CRITICAL/HIGH/MEDIUMの指摘なし。LOWは上記依存patch保守だけを保持。

## 最終結果

- CRITICAL/HIGH残件: 0
- 判定: passed（Epic統合後の全品質ゲート・独立監査完了）
